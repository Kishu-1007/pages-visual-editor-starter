// extends defaultThemeConfig.


import {
  defaultThemeConfig,
  type ThemeConfig,
} from "@yext/visual-editor";

/**
 * Project Theme Configuration
 *
 * Extend the Visual Editor default theme.
 * Override only when the project requires it.
 */
export const themeConfig: ThemeConfig = {
  ...defaultThemeConfig,

  // Add project-specific overrides here as needed.
  


};

export default themeConfig;

